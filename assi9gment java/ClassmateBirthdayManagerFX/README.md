# 🎂 ক্লাসমেট জন্মদিন ম্যানেজার (JavaFX + JDBC + MySQL)

এই প্রজেক্টে JavaFX দিয়ে ডেস্কটপ UI, JDBC দিয়ে ডাটাবেজ কানেকশন, এবং MySQL ব্যাকএন্ড ব্যবহার করে ক্লাসমেটদের জন্মদিন সংরক্ষণ/ম্যানেজ করা হয়েছে।  
**সব UI লেবেল ও আউটপুট বাংলা।**

---

## ✅ ফিচারসমূহ
- জন্মদিন **যোগ / আপডেট / ডিলিট**
- **আসন্ন জন্মদিন** (Sorted)
- **নাম** দিয়ে সার্চ
- **মাস** দিয়ে সার্চ
- **আজ জন্মদিন হলে নোটিফিকেশন** (Popup)

---

## 🛠️ টেকনোলজি
- Java 17+
- JavaFX
- JDBC
- MySQL

---

## 🗄️ ডাটাবেজ সেটআপ

MySQL এ নিচের SQL রান করুন:

```sql
CREATE DATABASE birthday_db CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;
USE birthday_db;

CREATE TABLE classmates (
  id INT AUTO_INCREMENT PRIMARY KEY,
  name VARCHAR(100) NOT NULL,
  dob DATE NOT NULL,
  phone VARCHAR(30),
  notes VARCHAR(255),
  created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);
```

---

## ⚙️ কনফিগারেশন (সবচেয়ে জরুরি)

`src/main/java/com/example/birthday/db/DB.java` ফাইলে আপনার MySQL এর **USER** এবং **PASS** সেট করুন:

```java
private static final String USER = "root";
private static final String PASS = "your_password";
```

---

## ▶️ রান করার নিয়ম

টার্মিনালে (প্রজেক্ট ফোল্ডারে) চালান:

```bash
mvn clean javafx:run
```

---

## 🖼️ স্ক্রিনশট

`screenshots/` ফোল্ডারে আপনার স্ক্রিনশটগুলো দিন এবং নিচের লিংকগুলো কাজ করবে।

### UI
![UI Screenshot](screenshots/ui.png)

### Database
![DB Screenshot](screenshots/db.png)
